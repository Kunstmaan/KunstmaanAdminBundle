language: php

php:
  - 5.3
  - 5.4

before_script:
    - pyrus install pear.netpirates.net/fDOMDocument
    - pyrus install pear.phpunit.de/hphpa
    - phpenv rehash
    - hphpa --checkstyle hphpa.xml .
    - curl -s http://getcomposer.org/installer | php
    - php composer.phar --dev install

notifications:
  email:
    - travis@kunstmaan.be
