language: php

php:
  - 5.3
  - 5.4

before_script:
    - pyrus channel-discover pear.phing.info
    - pyrus install phing/phing
    - phpenv rehash
    - phing -verbose

notifications:
  email:
    - travis@kunstmaan.be
