language: php

php:
  - 5.3
  - 5.4
  - 5.5

matrix:
  allow_failures:
    - php: 5.5


before_script:
  - composer selfupdate
  - composer --prefer-source --dev install

script: phpunit --debug

notifications:
  email:
    - travis@kunstmaan.be
